<Page
    x:Class="LiftDataManager.Views.SafetyComponentsRecordingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:SpeziControls="using:LiftDataManager.Controls"
    xmlns:collections="using:CommunityToolkit.Mvvm.Collections"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dbModels="using:LiftDataManager.Core.DataAccessLayer.Models"
    xmlns:linq="using:System.Linq"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <!--<DataTemplate x:Key="ItemTemplate" x:DataType="dbModels:Abbreviation">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource AccentAAFillColorSecondaryBrush}"
                    Style="{StaticResource ListTitleStyle}"
                    Text="{x:Bind ShortName}" />
                <TextBlock
                    Grid.Column="1"
                    FontSize="14"
                    FontWeight="Normal"
                    Style="{StaticResource BaseTextBlockStyle}"
                    Text="{x:Bind Name}"
                    TextWrapping="WrapWholeWords" />
            </Grid>
        </DataTemplate>-->
    </Page.Resources>
    <Grid x:Name="ContentArea">
        <TabView Grid.Row="1" IsAddTabButtonVisible="False">
            <TabView.Resources>
                <SolidColorBrush x:Key="TabViewItemHeaderBackgroundSelected" Color="Transparent" />
            </TabView.Resources>
            <TabView.TabItems>
                <TabViewItem Header="Aktueller Auftrag" IsClosable="False">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Margin="5"
                            FontSize="24"
                            Foreground="{ThemeResource AccentTextFillColorSecondaryBrush}"
                            Text="Equipments / Sicherheitskomponenten im Shape TraceLink Format" />
                        <Grid
                            Grid.Row="1"
                            Grid.Column="0"
                            Padding="5"
                            Style="{ThemeResource GridCardPanel}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <SpeziControls:ParameterTextBox
                                Grid.Row="0"
                                Grid.Column="0"
                                LiftParameter="{x:Bind ViewModel.ParameterDictionary['var_AuftragsNummer'], Mode=TwoWay}"
                                Opacity="0.5"
                                PlaceholderText="Auftragsnummer"
                                ReadOnly="True"
                                ShowDefaultHeader="True">
                                <ToolTipService.ToolTip>
                                    <TextBlock>
                                        <Run
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                            Text="Schreibgeschützer Parameter:" /><LineBreak />
                                        Auftragsnummer wird von Spezifikation übernommen</TextBlock>
                                </ToolTipService.ToolTip>
                            </SpeziControls:ParameterTextBox>
                            <SpeziControls:ParameterTextBox
                                Grid.Row="0"
                                Grid.Column="1"
                                LiftParameter="{x:Bind ViewModel.ParameterDictionary['var_FabrikNummer'], Mode=TwoWay}"
                                Opacity="0.5"
                                PlaceholderText="Fabriknummer"
                                ReadOnly="True"
                                ShowDefaultHeader="True">
                                <ToolTipService.ToolTip>
                                    <TextBlock>
                                        <Run
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                            Text="Schreibgeschützer Parameter:" /><LineBreak />
                                        Fabriknummer wird von Spezifikation übernommen</TextBlock>
                                </ToolTipService.ToolTip>
                            </SpeziControls:ParameterTextBox>
                            <SpeziControls:ParameterTextBox
                                Grid.Row="0"
                                Grid.Column="2"
                                LiftParameter="{x:Bind ViewModel.ParameterDictionary['var_SAISEquipment'], Mode=TwoWay}"
                                PlaceholderText="SAIS-Equipmentnummer"
                                ShowDefaultHeader="True" />
                            <SpeziControls:ParameterComboBox
                                Grid.Row="0"
                                Grid.Column="3"
                                Margin="0,0,5,0"
                                IsEnabled="False"
                                LiftParameter="{x:Bind ViewModel.ParameterDictionary['var_CeNummer'], Mode=TwoWay}"
                                ShowDefaultHeader="True">
                                <ToolTipService.ToolTip>
                                    <TextBlock>
                                        <Run
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                            Text="Schreibgeschützer Parameter:" /><LineBreak />
                                        CE-Nummer wird von Spezifikation übernommen</TextBlock>
                                </ToolTipService.ToolTip>
                            </SpeziControls:ParameterComboBox>
                            <SpeziControls:ParameterTextBox
                                Grid.Row="1"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                Margin="0,0,5,0"
                                LiftParameter="{x:Bind ViewModel.ParameterDictionary['var_Kennwort'], Mode=OneWay}"
                                Opacity="0.5"
                                PlaceholderText="Kennwort"
                                ReadOnly="True"
                                ShowDefaultHeader="True">
                                <ToolTipService.ToolTip>
                                    <TextBlock>
                                        <Run
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                            Text="Schreibgeschützer Parameter:" /><LineBreak />
                                        Kennwort wird von Spezifikation übernommen</TextBlock>
                                </ToolTipService.ToolTip>
                            </SpeziControls:ParameterTextBox>
                            <TextBox
                                Grid.Row="2"
                                Grid.Column="0"
                                MinHeight="92"
                                Margin="5,0,5,0"
                                AcceptsReturn="True"
                                Header="Bauvorhaben"
                                IsReadOnly="True"
                                Opacity="0.5"
                                PlaceholderText="Bauvorhaben"
                                Text="{x:Bind ViewModel.ParameterDictionary['var_Projekt'].Value, Mode=OneWay}"
                                TextWrapping="Wrap">
                                <ToolTipService.ToolTip>
                                    <TextBlock>
                                        <Run
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                            Text="Schreibgeschützer Parameter:" /><LineBreak />
                                        Bauvorhaben wird von Spezifikation übernommen</TextBlock>
                                </ToolTipService.ToolTip>
                            </TextBox>
                            <TextBox
                                Grid.Row="2"
                                Grid.Column="1"
                                MinHeight="92"
                                Margin="5,0,10,0"
                                AcceptsReturn="True"
                                Header="Betreiber"
                                IsReadOnly="True"
                                Opacity="0.5"
                                PlaceholderText="Betreiber"
                                Text="{x:Bind ViewModel.ParameterDictionary['var_Betreiber'].Value, Mode=OneWay}"
                                TextWrapping="Wrap">
                                <ToolTipService.ToolTip>
                                    <TextBlock>
                                        <Run
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                            Text="Schreibgeschützer Parameter:" /><LineBreak />
                                        Betreiber wird von Spezifikation übernommen</TextBlock>
                                </ToolTipService.ToolTip>
                            </TextBox>
                            <Button
                                Grid.Row="1"
                                Grid.Column="2"
                                Margin="5,5,5,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Bottom"
                                Content="Button1"
                                Style="{ThemeResource AccentButtonStyle}" />
                            <Button
                                Grid.Row="1"
                                Grid.Column="3"
                                Margin="5,5,5,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Bottom"
                                Content="Button2"
                                Style="{ThemeResource AccentButtonStyle}" />
                            <Button
                                Grid.Row="2"
                                Grid.Column="2"
                                Margin="5"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Top"
                                Content="Button3"
                                Style="{ThemeResource AccentButtonStyle}" />
                            <Button
                                Grid.Row="2"
                                Grid.Column="3"
                                Margin="5"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Top"
                                Content="Button4"
                                Style="{ThemeResource AccentButtonStyle}" />
                        </Grid>
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            Margin="5"
                            FontSize="24"
                            Foreground="{ThemeResource AccentTextFillColorSecondaryBrush}"
                            Text="Equipments im Shape TraceLink Format" />
                        <Grid
                            Grid.Row="3"
                            Grid.Column="0"
                            Padding="5"
                            Style="{ThemeResource GridCardPanel}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Montagebetrieb" />
                            <TextBlock Grid.Column="1" Text="Herstellernummer" />
                            <TextBlock Grid.Column="2" Text="Equipmentnummer" />
                            <TextBlock Grid.Column="3" Text="Straße" />
                            <TextBlock Grid.Column="4" Text="Hausnummer" />
                            <TextBlock Grid.Column="5" Text="Postleitzahl" />
                            <TextBlock Grid.Column="6" Text="Stadt" />
                            <TextBlock Grid.Column="7" Text="Land" />
                        </Grid>
                        <TextBlock
                            Grid.Row="4"
                            Grid.Column="0"
                            Margin="5"
                            FontSize="24"
                            Foreground="{ThemeResource AccentTextFillColorSecondaryBrush}"
                            Text="Sicherheitskomponenten im Shape TraceLink Format" />
                    </Grid>
                </TabViewItem>
                <TabViewItem Header="Equipments" IsClosable="False">
                    <TextBlock>Hallo22</TextBlock>
                </TabViewItem>
                <TabViewItem Header="Sicherheitskomponenten" IsClosable="False">
                    <TextBlock>Hallo33</TextBlock>
                </TabViewItem>
            </TabView.TabItems>
        </TabView>
    </Grid>
</Page>
